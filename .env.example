# Create this file and add your credentials
GRPC_URL=your_grpc_url_here
# X_TOKEN is optional - only required if your gRPC endpoint needs authentication
X_TOKEN=
# RPC URL for loading address lookup tables (e.g., https://api.devnet.solana.com or your QuickNode URL)
RPC_URL=https://api.devnet.solana.com
